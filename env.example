# Django Configuration
SECRET_KEY=tu-secret-key-aqui
DEBUG=True

# Database Configuration (Supabase PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# O usa estas variables individuales:
DB_NAME=postgres
DB_USER=postgres.xxxxx
DB_PASSWORD=tu-password
DB_HOST=aws-x-xx-xxx-x.pooler.supabase.com
DB_PORT=6543

# Railway (se configura automáticamente)
RAILWAY_PUBLIC_DOMAIN=

# Frontend URL (IMPORTANTE para CORS en producción)
FRONTEND_URL=https://tu-app-angular.vercel.app

# Railway (se configura automáticamente)
RAILWAY_PUBLIC_DOMAIN=

# Render (opcional)
RENDER_EXTERNAL_HOSTNAME=
